{
  "type": "object",
  "title": "Get_Slope",
  "description": "Gets a slope applied to a target",
  "additionalProperties": false,
  "properties": {
    "getter": {
      "description": "The instrument (and channel) to collect the slope from. The slope is the DC ramp slope of the gate bias voltage.",
      "$ref": "../lib/instrument_target.json#/definitions/InstrumentTarget"
    }
  },
  "returns": {
    "description": "The numeric value gotten in units of V/sec.",
    "type": "number"
  },
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/schemas/get_voltage.json"
}
