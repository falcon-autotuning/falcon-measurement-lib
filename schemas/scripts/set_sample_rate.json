{
  "type": "object",
  "title": "Set_Sample_Rate",
  "description": "Sets the sample rate on the ADC",
  "additionalProperties": false,
  "properties": {
    "getter": {
      "description": "The instrument (and channel) to collect the sample rate from.",
      "$ref": "../lib/instrument_target.json#/definitions/InstrumentTarget"
    },
    "sampleRate": {
      "description": "The sample rate to set in units of samples/msec.",
      "type": "number"
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/schemas/get_voltage.json"
}
