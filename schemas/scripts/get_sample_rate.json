{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/schemas/get_sample_rate.json",
  "type": "object",
  "title": "Get_Sample_Rate",
  "description": "Gets the number of samples of the ADC",
  "additionalProperties": false,
  "properties": {
    "getter": {
      "description": "The instrument (and channel) to collect the number of samples from",
      "$ref": "../lib/instrument_target.json#/definitions/InstrumentTarget"
    }
  },
  "returns": {
    "description": "The numeric value gotten in units of samples/msec.",
    "allOf": [
      {
        "$ref": "../lib/measurement_response.json#/definitions/MeasurementResponse"
      },
      {
        "description": "Annotates the concrete type of the MeasurementResponse value",
        "x-valueType": "number"
      }
    ]
  }
}
