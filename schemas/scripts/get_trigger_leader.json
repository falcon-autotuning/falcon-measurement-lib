{
  "type": "object",
  "title": "Get_Trigger_Leader",
  "description": "Gets the synchronous triggered leader DAC",
  "additionalProperties": false,
  "properties": {
    "getter": {
      "description": "The instrument (and channel) to check if the leader. The leader is in charge of syncing with the ADC and triggering the rest of the measurements.",
      "$ref": "../lib/instrument_target.json#/definitions/InstrumentTarget"
    }
  },
  "returns": {
    "description": "The numeric value gotten in units of V/sec.",
    "type": "bool"
  },
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/schemas/get_voltage.json"
}
